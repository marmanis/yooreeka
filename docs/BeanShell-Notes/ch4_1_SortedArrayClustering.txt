// --------------------------------------------------------
//    Clustering based on Array sorting
// --------------------------------------------------------
import *;

SFDataset ds = SFData.createDataset();

SortedArrayClustering.cluster(ds.getData());

